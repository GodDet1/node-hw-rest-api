const contactsServices = require('./contactsService');
const authService = require('./authService');
const fsServise = require('./fsServise');

module.exports = { contactsServices, authService, fsServise };
